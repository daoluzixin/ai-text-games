# 执行日志：初始部署与 Harness 搭建

日期：2026-06-11

---

## 阶段一：服务器连接建立

### 1. 宝塔面板连通性测试

- 收到服务器信息：阿里云 ECS `39.106.187.120`，宝塔面板端口 20426
- 初次测试 20426 端口不可达（安全组未放行）
- 用户放行安全组后，curl 返回 nginx 404，但浏览器访问成功加载宝塔登录页
- 面板账号 `xdcowtyf` / 密码 `57f819bc` 为宝塔 Web 登录账号（非系统用户）

### 2. SSH 公钥配置

- 尝试 SSH 连接，服务器只允许公钥认证（`Permission denied (publickey)`）
- `grep xdcowtyf /etc/passwd` 输出为空 → 确认该账号不是系统用户
- 通过宝塔面板终端，将本机公钥写入 `/root/.ssh/authorized_keys`
- 验证成功：`ssh root@39.106.187.120` 可正常连接

### 3. 服务器环境确认

- 系统：Ubuntu, 内核 5.15.0-179-generic, x86_64
- Python 3.10.12, pip 22.0.2, git 已安装
- 内存 1.6G，磁盘 40G（已用 5.2G）

---

## 阶段二：项目部署

### 4. 代码上传

- 使用 rsync 将项目同步到服务器 `/opt/ai-text-games/`
- 排除 `.git`、`__pycache__`、`.catpaw` 目录
- 共传输 18 个文件

### 5. 依赖安装

```bash
ssh root@39.106.187.120 "cd /opt/ai-text-games && pip3 install -r requirements.txt"
```

- 成功安装：fastapi, uvicorn, pydantic, python-dotenv, python-docx, openai 及其依赖
- pip 源：清华镜像（pypi.tuna.tsinghua.edu.cn）

### 6. systemd 服务创建与启动

- 创建 `/etc/systemd/system/ai-text-games.service`
- 首次启动失败：缺少 `python-multipart`（文件上传功能依赖）
- 安装 `pip3 install python-multipart` 后重启成功
- 服务状态：`active (running)`，监听 `0.0.0.0:8080`

### 7. 服务验证

- 服务器本地测试：`curl http://localhost:8080/api/games` → 返回 `[]`（正常，games 目录为空）
- 外网 8080 端口不可达 → 需用户在安全组放行

### 8. 游戏文件上传

- 通过 scp 上传 `46长公主模拟器1（上位版）.docx` 到 `/opt/ai-text-games/games/`
- 验证：API 返回 `[{"id":"f4e242cb","name":"长公主模拟器1（上位版）","avatar":"/api/avatars/avatar_03_wizard.svg"}]`

---

## 阶段三：Harness 搭建

### 9. 创建文档约束体系

按 harness-methodology.md 方法论，依次创建：

| 文件 | 职责 |
|------|------|
| `AGENTS.md` | 导航入口，首读顺序 + 按场景查文档 |
| `docs/agents/目元信息.md` | 项目背景、阶段、部署信息 |
| `docs/agents/核心架构.md` | 技术栈、目录结构、API 清单、数据流 |
| `docs/agents/工程约束.md` | 红线、代码风格、部署约束、Git 规范 |
| `docs/agents/自反馈工作流.md` | 6 步闭环验证流程 |

### 10. 创建执行计划体系

- `docs/exec-plans/README.md`：计划模板、状态流转规则、完成条件
- `docs/exec-plans/active/`：当前活跃计划（.gitkeep 保结构）
- `docs/exec-plans/completed/`：已完成计划
- `docs/exec-plans/activeLog/`：执行日志

### 11. 创建自动化守卫脚本

`scripts/harness-lint.sh` 覆盖 20 项检查：

- 文档完整性（6 项）：AGENTS.md + 4 份知识文档 + exec-plans README
- 目录结构（5 项）：active/completed/activeLog/scripts/avatars
- 架构约束（4 项）：单文件后端、.gitignore 包含敏感文件
- 代码检查（3 项）：server.py 语法、requirements.txt、index.html
- 配置检查（2 项）：.env.example、Dockerfile

### 12. 创建规则自动注入

`.catpaw/rules/` 下 3 个规则文件：

- `project-constraints.md`：6 条不可违反的项目约束
- `doc-maintenance.md`：代码改动后的文档同步规则
- `exec-plan-discipline.md`：执行计划纪律（先写计划、实时日志、运行态证据）

### 13. Git 初始化与本地忽略

- `git init` 初始化仓库
- `.git/info/exclude` 配置 harness 文件本地忽略（不提交到远程）

### 14. Lint 验证

```
📊 结果: 20 PASS / 0 FAIL
✅ 全部通过！
```

---

## 当前状态

| 项目 | 状态 |
|------|------|
| 服务器 SSH | ✅ 可连接 |
| 服务运行 | ✅ active (systemd) |
| 游戏加载 | ✅ 1 个游戏已识别 |
| 外网访问 | ⏳ 待安全组放行 8080 端口 |
| Harness | ✅ 20/20 PASS |
